version: '3.7'

services:
    v2ray:
    image: v2ray/official:latest
    container_name: v2ray
    hostname: v2ray
    restart: always
    command: 'v2ray -config=/etc/v2ray/config.json'    
    ports:
      - '127.0.0.1:8888:8888'
    volumes:
      - '/etc/v2ray:/etc/v2ray'
